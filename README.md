# chrome-search-exporter
Export chrome quick search engines into Firefox bookmarks

## Build

Just like any other Rust project: 
```bash
cargo build
```

## Usage
```bash
target/debug/chrome-search-exporter ~/.config/google-chrome/Default/Web\ Data > bookmarks.html
```
